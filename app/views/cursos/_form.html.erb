<%= form_with(model: curso, local: true) do |form| %>
  <% if curso.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(curso.errors.count, "error") %> prohibited this curso from being saved:</h2>

      <ul>
      <% curso.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="field">
    <%= form.label :disciplinas_id %>
    <%= form.select(:disciplinas_id, 
                    Disciplinas.all.collect {|d| [d.nome, d.id]}) %>
</div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
