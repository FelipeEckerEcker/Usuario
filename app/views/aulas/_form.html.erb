<%= form_with(model: aula, local: true) do |form| %>
  <% if aula.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(aula.errors.count, "error") %> prohibited this aula from being saved:</h2>

      <ul>
      <% aula.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :disciplina %>
    <%= form.text_field :disciplina %>
  </div>

  <div class="field">
    <%= form.label :data %>
    <%= form.date_select :data %>
  </div>

  <div class="field">
    <%= form.label :objetivo %>
    <%= form.text_field :objetivo %>
  </div>

  <div class="field">
    <%= form.label :realizado %>
    <%= form.text_field :realizado %>
  </div>

  <div class="field">
    <%= form.label :alunos_id %>
    <%= form.select(:alunos_id, 
                    Aluno.all.collect {|a| [a.nome, a.id]}) %>
</div>

<div class="field">
<%= form.label :professores_id %>
<%= form.select(:professores_id, 
                Professor.all.collect {|p| [p.nome, p.id]}) %>
</div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
